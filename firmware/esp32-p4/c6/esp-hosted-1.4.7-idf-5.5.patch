diff --git a/slave/main/slave_control.c b/slave/main/slave_control.c
--- a/slave/main/slave_control.c
+++ b/slave/main/slave_control.c
@@ -23,7 +23,6 @@
 #include "esp_ota_ops.h"
 #include "esp_hosted_rpc.h"
 #include "esp_hosted_transport.h"
-#include "esp_hosted_bitmasks.h"
 
 /* Slave-side ESP-IDF version detection for bitmasks compatibility */
 #include "esp_idf_version.h"
@@ -39,6 +38,8 @@
 #define H_WIFI_VHT_FIELDS_AVAILABLE 0
 #endif
 
+#include "esp_hosted_bitmasks.h"
+
 /* Slave-side: Enable reserved field decoding by default for compatibility */
 #define H_DECODE_WIFI_RESERVED_FIELD 1
 
@@ -1022,7 +1023,11 @@ static int req_wifi_get_config(Rpc *req)
 		if (p_a_sta->transition_disable)
 			H_SET_BIT(STA_TRASITION_DISABLED_BIT, p_c_sta->bitmask);
 
+#if H_WIFI_NEW_RESERVED_FIELD_NAMES
+		WIFI_CONFIG_STA_SET_RESERVED_VAL(p_a_sta->reserved1, p_c_sta->bitmask);
+#else
 		WIFI_CONFIG_STA_SET_RESERVED_VAL(p_a_sta->reserved, p_c_sta->bitmask);
+#endif
 
 		p_c_sta->sae_pwe_h2e = p_a_sta->sae_pwe_h2e;
 		p_c_sta->failure_retry_cnt = p_a_sta->failure_retry_cnt;

--- a/esp32_p4_wifi6_touch_lcd_4_3.c
+++ b/esp32_p4_wifi6_touch_lcd_4_3.c
@@ -487,7 +487,9 @@ esp_err_t bsp_display_new_with_handles(const bsp_display_config_t *config, bsp_l
     esp_lcd_dsi_bus_config_t bus_config = {
         .bus_id = 0,
         .num_data_lanes = BSP_LCD_MIPI_DSI_LANE_NUM,
-        .phy_clk_src = MIPI_DSI_PHY_PLLREF_CLK_SRC_XTAL,
+        // Let ESP-IDF select the revision-compatible PLL reference:
+        // PLL_F20M for pre-v3 silicon and XTAL for rev3.x or newer.
+        .phy_clk_src = 0,
         .lane_bit_rate_mbps = BSP_LCD_MIPI_DSI_LANE_BITRATE_MBPS,
     };
     ESP_RETURN_ON_ERROR(esp_lcd_new_dsi_bus(&bus_config, &mipi_dsi_bus), TAG, "New DSI bus init failed");
